---
trigger: always_on
---

Never assume.

Never hallucinate missing requirements.

If documentation exists, documentation wins.

If documentation conflicts with implementation, report the conflict.

Never silently change architecture.

Never silently change naming conventions.

Never silently change project structure.